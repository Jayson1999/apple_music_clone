import 'package:flutter/material.dart';

class ListenNowTab extends StatelessWidget {
  const ListenNowTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('LISTEN NOW');
  }
}
